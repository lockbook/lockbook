name: Publish Crate

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      package:
        description: 'Name of the crate to publish'
        required: false
        default: 'lb-rs'

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      CRATES_IO_API_TOKEN: ${{ secrets.API_KEY }}
      
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable

      - name: Publish crate
        run: |
          cargo run -p releaser -- publish-crate ${{github.event.inputs.package}}
