[env]
# aws-lc-sys (via rig → reqwest → rustls) emits ___chkstk_darwin, which only
# exists on modern iOS; without this rustc links at its default floor of iOS
# 10.0 and the symbol comes up undefined. 16.0 matches Package.swift's .iOS(.v16).
IPHONEOS_DEPLOYMENT_TARGET = "16.0"
